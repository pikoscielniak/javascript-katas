"use strict";

var expect = require('chai').expect,
  binarySearch = require('../search/binarySearch');

describe("binarySearch", function () {
  describe("rank", function () {
    it("should exists", function () {
      expect(binarySearch.rank).to.be.ok;
    });
    
  });
});
